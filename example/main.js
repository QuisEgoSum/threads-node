const {MainThread} = require('../index')



const thread = new MainThread(
    {
        threads: [
            {
                options: {
                    
                }
            }
        ]
    }
)